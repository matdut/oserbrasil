import React from 'react';
import './Marker.css';

const Marker = () => {
   // const { color, name, id } = props;
    return (
      <div className="marker"
       
      />
    );
  };

  export default Marker;